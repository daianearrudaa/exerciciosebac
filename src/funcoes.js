"use strict";
function calculaArea(base, altura) {
    return base * altura;
}
const calcula = (base, altura) => base * altura;
var casting;
(function (casting) {
    let idade = 25;
    idade.toFixed();
    idade.length;
})(casting || (casting = {}));
